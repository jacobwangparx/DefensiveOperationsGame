package home.game.defensiveOperations.view.components.interfaces {
	import flash.display.LoaderInfo;
	/**
	 * <p>
	 * Interface for the main so that the preloader can tell it when to start
	 * </p>
	 *
	 * @class IMain
	 * @author Jake
	 * @date 
	 * @version 1.0
	 * @see
	 */
	
	public interface IMain {
		function init(loaderInfo:LoaderInfo):void;
	}	
}